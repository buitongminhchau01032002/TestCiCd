name: Tests

on: push

jobs:
  tests:
    name: Unit Testing
    runs-on: [windows-latest, ubuntu-latest]
    steps:
      - uses: actions/checkout@v3
      - run: dotnet build
      - run: dotnet test
